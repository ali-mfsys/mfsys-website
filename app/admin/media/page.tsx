"use client";import {useState} from "react";export default function Media(){const [type,setType]=useState("ALL");const [open,setOpen]=useState(false);const types=["ALL","IMAGE","VIDEO","DOCUMENT"];return <main className="admin-content"><div className="admin-kicker">CONTENT / MEDIA</div><div className="admin-page-title"><div><h1>Media Library</h1><p>Manage optimized images, sector videos, documents and accessibility metadata.</p></div><button className="admin-primary" onClick={()=>setOpen(true)}>+ Add media</button></div><div className="media-toolbar">{types.map(x=><button key={x} className={type===x?"active":""} onClick={()=>setType(x)}>{x}</button>)}</div><div className="media-drop"><div className="empty-icon">↑</div><h2>Upload your visual library</h2><p>Connect object storage/CDN for production uploads. Keep hero video lightweight and serve responsive poster images to protect Core Web Vitals.</p><div className="media-guidance"><span>Images · WebP/AVIF</span><span>Video · MP4/WebM</span><span>Documents · PDF</span></div></div>{open&&<div className="modal"><div className="modal-card"><h2>Add media asset</h2><label className="editor-field">Asset URL<input placeholder="https://cdn.example.com/asset"/></label><label className="editor-field">Filename<input placeholder="hero-banking.mp4"/></label><label className="editor-field">Type<select defaultValue="VIDEO"><option>IMAGE</option><option>VIDEO</option><option>DOCUMENT</option></select></label><label className="editor-field">Alt text<input placeholder="Describe the asset for accessibility"/></label><div className="editor-actions"><button className="admin-secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="admin-primary" onClick={()=>setOpen(false)}>Save metadata</button></div></div></div>}</main>