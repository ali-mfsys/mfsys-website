"use client";import {FormEvent,useState} from "react";export default function NewInsight(){const [result,setResult]=useState<any>(null);const [busy,setBusy]=useState(false);async function analyze(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/ai/seo",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({title:f.get("title"),content:f.get("body"),focusKeyword:f.get("keyword"),type:f.get("type")})});setResult(await r.json());setBusy(false)}return <main className="admin-content"><div className="admin-kicker">CONTENT / INSIGHTS / NEW</div><h1>Create insight</h1><p className="admin-intro">Create SEO-ready research, case studies, articles, reports or news.</p><form className="admin-panel editor-form" onSubmit={analyze}><label className="editor-field">Title<input name="title" placeholder="Insight title" required/></label><label className="editor-field">Type<select name="type" defaultValue="ARTICLE"><option>RESEARCH</option><option>CASE STUDY</option><option>ARTICLE</option><option>REPORT</option><option>NEWS</option></select></label><label className="editor-field">Focus keyword<input name="keyword" placeholder="e.g. AI credit scoring"/></label><label className="editor-field">Excerpt<textarea name="excerpt" rows={3} placeholder="Short summary"/></label><label className="editor-field">Body<textarea name="body" rows={14} placeholder="Write your content here..." required/></label><div className="editor-actions"><button type="button" className="admin-secondary">Save draft</button><button className="admin-primary" disabled={busy}>{busy?"Analyzing…":"Analyze with AI →"}</button></div>{result?.analysis&&<section className="ai-review"><div><strong>SEO Readiness</strong><span className="ai-score">{result.analysis.score}/100</span></div><p>{result.analysis.wordCount} words · {result.analysis.keywordMentions} focus-keyword mentions</p>{result.analysis.recommendations.map((x:string)=><div className="ai-rec" key={x}>↗ {x}</div>)}<div className="ai-suggestion"><strong>Suggested meta description</strong><p>{result.analysis.suggestedDescription}</p></div></section>}</form></main>}