"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";export default function Login(){const router=useRouter();const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setBusy(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(r.ok)router.push("/admin");else setError("Invalid credentials. Please try again.");setBusy(false)}return <main className="admin-login"><div className="login-card"><div className="admin-kicker">MFSYS CONTENT STUDIO</div><h1>Sign in</h1><p>Secure access for authorized MFSYS administrators and editors.</p><form onSubmit={submit}><label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" minLength={8} required/></label>{error&&<div role="alert" className="login-error">{error}</div>}<button className="admin-primary" disabled={busy}>{busy?"Signing in…":"Continue securely →"}</button></form><small>Production access requires MFA to be enabled in the identity layer.</small></div></main>}