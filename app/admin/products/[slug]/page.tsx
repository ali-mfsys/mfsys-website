import {productData} from "../../../../lib/product-data";export default async function ProductEditor({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const product=productData.find(p=>p.href.endsWith("/"+slug));return <main className="admin-content"><div className="admin-kicker">CONTENT / PRODUCT EDITOR</div><h1>{product?.name??"Product"}</h1><p className="admin-intro">Manage the product proposition, capabilities, outcomes and SEO metadata.</p><div className="editor-grid"><section className="admin-panel"><h2>Product proposition</h2><label className="editor-field">Name<input defaultValue={product?.name}/></label><label className="editor-field">Tagline<input defaultValue={product?.tagline}/></label><label className="editor-field">Description<textarea rows={7} defaultValue={product?.description}/></label><h2>Capabilities</h2><div className="builder-list">{product?.capabilities.map(x=><div className="builder-item" key={x}><span>{x}</span><button>Edit</button></div>)}</div></section><aside className="admin-panel"><h2>Publishing</h2><span className="status-pill">PUBLISHED</span><p>Changes should be saved as draft and pass review before production publishing.</p><button className="admin-primary">Save draft</button></aside></div></main>}