"use client";import Link from "next/link";import {useState} from "react";import {productData} from "../../../lib/product-data";export default function Products(){const [create,setCreate]=useState(false);return <main className="admin-content"><div className="admin-kicker">CONTENT / PRODUCTS</div><div className="admin-page-title"><div><h1>Products</h1><p>Manage product propositions, capabilities, outcomes and SEO.</p></div><button className="admin-primary" onClick={()=>setCreate(true)}>+ New product</button></div><div className="admin-table">{productData.map(p=><div className="admin-row" key={p.name}><div><strong>{p.name}</strong><small>{p.eyebrow}</small></div><span className="status-pill">Published</span><Link href={p.href}>Preview ↗</Link><button>Edit</button></div>)}</div>{create&&<div className="modal"><div className="modal-card"><h2>New product</h2><p>Product records are created through the secured CMS API.</p><button className="admin-secondary" onClick={()=>setCreate(false)}>Close</button></div></div>}</main>}