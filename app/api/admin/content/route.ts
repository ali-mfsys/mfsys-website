import {NextResponse} from "next/server";import {z} from "zod";import {db} from "../../../lib/db";import {pages} from "../../../lib/db/schema";import {eq} from "drizzle-orm";const pageSchema=z.object({slug:z.string().min(1).max(120).regex(/^[a-z0-9-]+$/),title:z.string().min(1).max(180),seoTitle:z.string().max(180).optional(),seoDescription:z.string().max(320).optional()});export async function GET(){const rows=await db.select().from(pages);return NextResponse.json(rows);}export async function POST(req:Request){const parsed=pageSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Invalid content payload"},{status:400});const [row]=await db.insert(pages).values(parsed.data).returning();return NextResponse.json(row,{status:201});}export async function PATCH(req:Request){const body=await req.json();const id=z.string().uuid().parse(body.id);const data=pageSchema.partial().parse(body);delete (data as any).id;const [row]=await db.update(pages).set({...data,updatedAt:new Date()}).where(eq(pages.id,id)).returning();return NextResponse.json(row);}