import {NextResponse} from "next/server";import {z} from "zod";import {signSession,verifyPassword} from "../../../../lib/auth";const schema=z.object({email:z.string().email(),password:z.string().min(8)});export async function POST(req:Request){const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Invalid credentials"},{status:401});if(parsed.data.email.toLowerCase()!==(process.env.ADMIN_EMAIL??"").toLowerCase()||!verifyPassword(parsed.data.password))return NextResponse.json({error:"Invalid credentials"},{status:401});const response=NextResponse.json({ok:true});response.cookies.set("mfsys_admin_session",signSession({email:parsed.data.email,role:"SUPER_ADMIN",exp:Date.now()+8*60*60*1000}),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:8*60*60});return response}